<Window x:Class="PIDDesigner.App.DesignerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:runtime="clr-namespace:PIDDesigner.Runtime;assembly=PIDDesigner.Runtime"
        Title="PID Designer" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Toolbox (Left) -->
        <Border BorderBrush="Gray" BorderThickness="0,0,1,0" Background="#FAFAFA">
            <StackPanel Margin="10">
                <TextBlock Text="Toolbox" FontWeight="Bold" Margin="0,0,0,10"/>
                <Button Content="Add Pump" x:Name="BtnAddPump" Click="BtnAddPump_Click" Height="30" Margin="0,0,0,5" />
                <Button Content="Add Valve" x:Name="BtnAddValve" Click="BtnAddValve_Click" Height="30" Margin="0,0,0,10" />
                <Button Content="Connect Components" x:Name="BtnConnect" Click="BtnConnect_Click" Height="30" Margin="0,0,0,10" Background="#FFF9C4" />
                <Button Content="Save Design" x:Name="BtnSave" Click="BtnSave_Click" Height="30" Background="#E1F5FE" />
                
                <TextBlock x:Name="TxtStatus" Text="Ready" Margin="0,10,0,0" Foreground="Gray" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
        
        <!-- Drawing Canvas (Right) -->
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <runtime:PIDViewer x:Name="ViewerControl" Width="2000" Height="2000" />
        </ScrollViewer>
    </Grid>
</Window>
